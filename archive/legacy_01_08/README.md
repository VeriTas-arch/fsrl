# Legacy 01-08 Archive

这里完整保存早期按编号组织的探索，不是当前研究主线。它们的共同问题是显式 score / edge memory，或将 global rank commitment、离散 hypothesis、rank-attractor 等底层机制预先手写，从而无法证明 plastic RNN 是必要瓶颈。

其中 01-04 是显式 score / memory 系列；05-08 是 active-rank、meta-plastic rank-attractor、ablation 与 auxiliary-target 系列。请以仓库根目录的 `README_v3_results_updated.md` 和 `STRUCTURE.md` 为当前机制解释的依据。

所有原始源码、结果、阶段报告、diff、patch 和 smoke 脚本均已原样保留。
