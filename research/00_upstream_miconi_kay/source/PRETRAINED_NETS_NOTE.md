# Pretrained network files

The uploaded original `fsrl.zip` contained `net_active.dat` and `net_passive.dat` from the meta-training paper code. They are not copied into the git-ready repository to keep the remote lightweight. The stage preserves all runnable Python source files and environment files needed to reproduce or continue work.
