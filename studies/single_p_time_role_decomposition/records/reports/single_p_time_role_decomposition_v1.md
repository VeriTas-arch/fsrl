# Frozen single-P time-role decomposition

The diagnostic is **noninterpretable**. The parent clean-single-P outcome remains
`time_removal_failure`, and no time-role mechanism label is assigned.

The final source-locked execution reconstructed the parent through its exact evaluation
adapter and separately compared that fast-weight state with the 32-channel clean direct
state used by the proposed interventions. The first failure occurred for seed 3012,
panel 1, `clean_no_time`, Ae, generic episodes: one element of terminal (P) exceeded
the frozen `atol=1e-5, rtol=1e-5` criterion. Its absolute error was
`2.670288e-05`, or `1.1840417` times the allowed elementwise tolerance.

This is small as a floating-point discrepancy but decisive for this protocol. Stage 2
and Stage 3 were intended to interpret terminal (P), write trajectories, and probes.
Those claims require one numerically stable arithmetic authority. Therefore Stage 1
is not interpreted, Stages 2–3 are not executed, and `scale_sufficient`, support-time,
query-time, state-dependence, confidence-link, and heterogeneity labels remain unset.

The four noninterpretable attempts and every pre-outcome repair are retained. No
runtime result or scientific array was produced by any attempt, and no intervention or
mechanism estimate was printed or inspected. A successor would need to freeze one
arithmetic path for both the parent comparison and all (P)-trajectory interventions;
it cannot repair this frozen study post hoc.
