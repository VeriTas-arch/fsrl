# Additive-by-residual field reassembly

<!-- fsrl-doc role=generated-navigation source=studies/global_policy_field_reassembly/study.toml -->

> [!NOTE]
> **Generated navigation.**
>
> - **Authority:** `studies/global_policy_field_reassembly/study.toml`
> - **Rebuild:** `direnv exec . python -m fsrl.infra.study_registry build`
> - **Edit:** do not edit this README directly.
>
> `review_state = "indexed"` means the records are organized and
> structurally checked. This page is navigation, not reviewed cross-study
> synthesis or independent scientific evidence.

[Back to the study registry](../README.md)

- **Status:** `mixed`
- **Review state:** `indexed`
- **Study ID:** `global_policy_field_reassembly`

## Scientific role

**Question.** Does additive source, residual source, or their fixed-sigmoid context
close the neural/posterior slope gap?

**Finding.** Additive source and norm-matched shape effects are positive, the
fixed-sigmoid interaction is negative, and neither component alone closes the
comparator.

**Claim boundary.** Pre-sigmoid interaction is exactly zero; the probability-space
context effect is not a network-internal multiplicative circuit.

## Frozen records

- `registered_contract` —
  [benchmarks/global_policy_field_reassembly_v1.json](records/benchmarks/global_policy_field_reassembly_v1.json)
  (`sha256:8b2f6785b6c9`)
- `execution_lock` —
  [benchmarks/global_policy_field_reassembly_v1.lock.json](records/benchmarks/global_policy_field_reassembly_v1.lock.json)
  (`sha256:4acf1908a208`)
- `report` —
  [docs/global_policy_field_reassembly_v1.md](records/docs/global_policy_field_reassembly_v1.md)
  (`sha256:d06b3cd69cae`)
- `frozen_result` —
  [results/global_policy_field_reassembly_v1.json](records/results/global_policy_field_reassembly_v1.json)
  (`sha256:de1419e1dc61`)

## Provenance rule

Files under `records/` are byte-preserving relocations. Their former paths,
hashes, sizes, and source ref are recorded in `study.toml` and the global
migration map. New interpretation belongs in this capsule or `synthesis/`;
the frozen records themselves are not rewritten.
Commands and relative links inside a frozen report describe its historical
checkout. Use the maintained workflow for current commands, or the snapshot
replay guide for an exact detached-worktree replay.

Add a `figures/` directory only when this study has a promoted, reproducible
study-level figure. Cross-study paper figures belong in `synthesis/figures/`.
