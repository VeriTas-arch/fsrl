# Fresh-backbone field-fingerprint replication

<!-- fsrl-doc role=generated-navigation source=studies/global_policy_field_fingerprint_replication/study.toml -->

> [!NOTE]
> **Generated navigation.**
>
> - **Authority:** `studies/global_policy_field_fingerprint_replication/study.toml`
> - **Rebuild:** `direnv exec . python -m fsrl.infra.study_registry build`
> - **Edit:** do not edit this README directly.
>
> `review_state = "indexed"` means the records are organized and
> structurally checked. This page is navigation, not reviewed cross-study
> synthesis or independent scientific evidence.

[Back to the study registry](../README.md)

- **Status:** `confirmed`
- **Review state:** `indexed`
- **Study ID:** `global_policy_field_fingerprint_replication`

## Scientific role

**Question.** Do the additive-source, equal-energy shape, and fixed-sigmoid interaction
fingerprints replicate on new backbones?

**Finding.** A>0, Q_shape>0, and I<0 replicate independently on seeds 2106 and 2107
after joint artifact locking.

**Claim boundary.** R remains unresolved and cannot be upgraded to equivalence; no
single field component is shown sufficient.

## Frozen records

- `artifact_lock` —
  [benchmarks/global_policy_field_fingerprint_replication_v1.artifact_lock.json](records/benchmarks/global_policy_field_fingerprint_replication_v1.artifact_lock.json)
  (`sha256:14bc424da38e`)
- `registered_contract` —
  [benchmarks/global_policy_field_fingerprint_replication_v1.json](records/benchmarks/global_policy_field_fingerprint_replication_v1.json)
  (`sha256:de83db0f30a9`)
- `execution_lock` —
  [benchmarks/global_policy_field_fingerprint_replication_v1.lock.json](records/benchmarks/global_policy_field_fingerprint_replication_v1.lock.json)
  (`sha256:8554497e3b99`)
- `report` —
  [docs/global_policy_field_fingerprint_replication_v1.md](records/docs/global_policy_field_fingerprint_replication_v1.md)
  (`sha256:553a8b6637f6`)
- `frozen_result` —
  [results/global_policy_field_fingerprint_replication_v1.json](records/results/global_policy_field_fingerprint_replication_v1.json)
  (`sha256:21592db1f9dc`)

## Provenance rule

Files under `records/` are byte-preserving relocations. Their former paths,
hashes, sizes, and source ref are recorded in `study.toml` and the global
migration map. New interpretation belongs in this capsule or `synthesis/`;
the frozen records themselves are not rewritten.
Commands and relative links inside a frozen report describe its historical
checkout. Use the maintained workflow for current commands, or the snapshot
replay guide for an exact detached-worktree replay.

Add a `figures/` directory only when this study has a promoted, reproducible
study-level figure. Cross-study paper figures belong in `synthesis/figures/`.
