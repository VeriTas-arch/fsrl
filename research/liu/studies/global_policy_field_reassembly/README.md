# Additive-by-residual field reassembly

> [!NOTE]
> This page is generated from `research/liu/catalog.json`. Edit the
> catalog, then run `direnv exec . python -m fsrl.liu_catalog build`.
> Historical files remain canonical at their original paths.

[Back to Liu research guide](../../README.md)

- **Status:** `mixed`
- **Study ID:** `global_policy_field_reassembly`
- **Chapter:** Global-policy mismatch and comparator boundary

## Scientific role

**Question.** Does additive source, residual source, or their fixed-sigmoid context close the neural/posterior slope gap?

**Finding.** Additive source and norm-matched shape effects are positive, the fixed-sigmoid interaction is negative, and neither component alone closes the comparator.

**Claim boundary.** Pre-sigmoid interaction is exactly zero; the probability-space context effect is not a network-internal multiplicative circuit.

## Canonical files

- `registered_contract` — [benchmarks/global_policy_field_reassembly_v1.json](../../../../benchmarks/global_policy_field_reassembly_v1.json)
- `execution_lock` — [benchmarks/global_policy_field_reassembly_v1.lock.json](../../../../benchmarks/global_policy_field_reassembly_v1.lock.json)
- `report` — [docs/global_policy_field_reassembly_v1.md](../../../../docs/global_policy_field_reassembly_v1.md)
- `frozen_result` — [results/global_policy_field_reassembly_v1.json](../../../../results/global_policy_field_reassembly_v1.json)

## Path policy

The files above remain canonical at their registered historical paths. This
capsule is the stable human-facing home for the study. A future study may put
its canonical files inside its capsule from inception, but relocating these
frozen files would require a separately versioned provenance migration.
