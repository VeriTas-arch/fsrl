# Single-P direct-authority baseline result

Registered outcome: `baseline_incompatible`.

The repaired Phase-1 command first reached the registered external-compatibility gate on seed 3011, panel 1, `time_retained_control`, cell A0. Its two independently loaded 32D eager replays had already passed the required bitwise comparison, and its intact generic and Liu margins had passed the frozen `atol=1e-5, rtol=1e-5` checks. The recomputed per-episode generic cross-entropy then differed from the frozen parent raw array by at most `5.2877695888575715e-08`, exceeding the prospectively frozen `1e-10` probability/CE tolerance.

Execution therefore stopped before writing any baseline artifact member. No panel summary, parent classification comparison, terminal-P authority lock, Stage-1 scale estimate, Stage-2 time intervention, Stage-3 probe estimate, or mechanism label was produced.

The earlier `baseline_attempt1` is a separate zero-unit implementation failure caused by incompatible reference-verifier metadata semantics. Its append-only repair did not alter this numerical gate, any tolerance, or any scientific estimand.

This result does not show that the 32D direct path changes the parent scientific classifications; those comparisons were not reached. It shows only that the registered `1e-10` per-subject probability/CE identity requirement is not satisfied under the new eager direct arithmetic authority. The parent clean-single-P outcome remains `time_removal_failure`, and the predecessor and successor assign no time-role mechanism label.
